from util import Util

class kerneloot:
    def __init__(self):
        self.kernel_version = 1

    def boot_ernel(ctx):
        print(f"Runnning AIOS Kernel {ctx.kernel_version}")

        kernel_boot_strings = []
        runner = Util.RunnerBoot()

        for s in kernel_boot_strings: