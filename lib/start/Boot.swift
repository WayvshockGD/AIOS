class Boot {
    var hasStarted: Bool = false;

    func start() -> Any {
        
        if self.hasStarted == false {
            print("Phone has already started!")
        } else {
            
        }
        
        
    }
}